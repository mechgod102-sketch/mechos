name: Publish MechOS v0.3.0 Hotfix 26

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/mechos-oobe-start-v26.sh'
      - 'scripts/mechos-hotfix-0.3.0-26-apply.sh'
      - 'scripts/build-hotfix-0.3.0-26.sh'
      - 'scripts/validate-hotfix-0.3.0-26.sh'
      - '.github/workflows/publish-hotfix-0.3.0-26.yml'

permissions:
  contents: write

concurrency:
  group: mechos-hotfix-26-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout MechOS
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install bundle tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y zstd
      - name: Validate Hotfix 26 source
        run: |
          set -euo pipefail
          bash scripts/validate-hotfix-0.3.0-26.sh
      - name: Publish Hotfix 26 to Update Center stable channel
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            bash scripts/validate-hotfix-0.3.0-26.sh
            bash scripts/build-hotfix-0.3.0-26.sh

            BUNDLE=updates/bundles/MechOS-0.3.0-hotfix.26-update.tar.zst
            (cd "$(dirname "$BUNDLE")" && sha256sum -c "$(basename "$BUNDLE").sha256")
            CONTENTS="$(tar --zstd -tf "$BUNDLE")"
            grep -Fxq './usr/local/bin/mechos-oobe-start' <<< "$CONTENTS"
            grep -Fxq './usr/local/libexec/mechos-hotfix-0.3.0-26-apply' <<< "$CONTENTS"
            grep -Fxq './usr/lib/systemd/system/mechos-hotfix-0.3.0-26.service' <<< "$CONTENTS"
            grep -Fxq './etc/systemd/system/multi-user.target.wants/mechos-hotfix-0.3.0-26.service' <<< "$CONTENTS"
            grep -Fxq './usr/local/libexec/mechos-hotfix-0.3.0-25-apply' <<< "$CONTENTS"
            grep -Fxq './usr/local/bin/mechos-update-helper' <<< "$CONTENTS"

            tar --zstd -xOf "$BUNDLE" ./usr/local/bin/mechos-oobe-start | grep -Fq 'MECHOS_OOBE_START_V26'
            tar --zstd -xOf "$BUNDLE" ./usr/local/bin/mechos-oobe-start | grep -Fq 'systemctl --user show-environment'
            tar --zstd -xOf "$BUNDLE" ./usr/local/bin/mechos-oobe-start | grep -Fq 'run_candidate xcb'

            python3 - <<'PY'
          import json
          from pathlib import Path
          data=json.loads(Path('updates/stable.json').read_text())
          assert data['version']=='0.3.0-hotfix.26', data
          assert data['requires_reboot'] is True, data
          assert data['bundle_url'].endswith('MechOS-0.3.0-hotfix.26-update.tar.zst'), data
          assert 'First System Setup' in data['notes'], data
          PY

            git add updates/bundles/MechOS-0.3.0-hotfix.26-update.tar.zst updates/bundles/MechOS-0.3.0-hotfix.26-update.tar.zst.sha256 updates/stable.json
            if git diff --cached --quiet; then
              echo 'Hotfix 26 is already published.'
              exit 0
            fi
            git commit -m 'Publish MechOS v0.3.0 Hotfix 26'
            if git push origin HEAD:main; then exit 0; fi
            echo "Publish race on attempt ${attempt}; rebuilding from latest main."
            sleep $((attempt * 2))
          done
          echo 'Unable to publish Hotfix 26 after three synchronized rebuild attempts.' >&2
          exit 1
