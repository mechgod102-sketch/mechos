name: Publish MechOS Hotfix 32

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/publish-hotfix-32.yml'

permissions:
  contents: write

concurrency:
  group: publish-mechos-hotfix-32
  cancel-in-progress: false

jobs:
  publish-hotfix-32:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout MechOS
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install bundle tools
        run: |
          sudo apt-get update
          sudo apt-get install -y zstd util-linux
      - name: Validate Hotfix 32 sources
        run: bash scripts/validate-hotfix-0.3.0-32.sh
      - name: Build cumulative Hotfix 32 bundle
        run: bash scripts/build-hotfix-0.3.0-32.sh
      - name: Verify Hotfix 32 publication files
        run: |
          set -euo pipefail
          B=updates/bundles/MechOS-0.3.0-hotfix.32-update.tar.zst
          test -s "$B"
          test -s "$B.sha256"
          (cd updates/bundles && sha256sum -c MechOS-0.3.0-hotfix.32-update.tar.zst.sha256)
          tar --zstd -tf "$B" | grep -F './usr/local/bin/mechscope-session'
          tar --zstd -tf "$B" | grep -F './usr/local/libexec/mechos-mechscope-safe-launch-v31'
          tar --zstd -tf "$B" | grep -F './usr/local/libexec/mechos-session-autostart-v31'
          tar --zstd -tf "$B" | grep -F './usr/local/libexec/mechos-hotfix-0.3.0-32-apply'
          python3 - <<'PY'
          import hashlib,json
          from pathlib import Path
          p=Path('updates/stable.json')
          data=json.loads(p.read_text())
          assert data['channel']=='stable'
          assert data['version']=='0.3.0-hotfix.32'
          assert data['requires_reboot'] is True
          b=Path('updates/bundles/MechOS-0.3.0-hotfix.32-update.tar.zst')
          assert hashlib.sha256(b.read_bytes()).hexdigest()==data['bundle_sha256']
          PY
      - name: Commit stable Hotfix 32
        run: |
          set -euo pipefail
          git config user.name 'MechOS Hotfix Publisher'
          git config user.email 'actions@users.noreply.github.com'
          git add updates/stable.json \
            updates/bundles/MechOS-0.3.0-hotfix.32-update.tar.zst \
            updates/bundles/MechOS-0.3.0-hotfix.32-update.tar.zst.sha256
          if git diff --cached --quiet; then
            echo 'Hotfix 32 publication files are already committed.'
            exit 0
          fi
          git commit -m 'release: publish MechOS Hotfix 32'
          git push origin HEAD:main
