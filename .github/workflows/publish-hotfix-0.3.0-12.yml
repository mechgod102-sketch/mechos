name: Publish MechOS v0.3.0 Hotfix 12

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/mechos-vm-mode-runtime-hotfix5.sh'
      - 'scripts/mechos-mode-launch-hotfix10.sh'
      - 'scripts/mechos-hotfix-0.3.0-12-apply.sh'
      - 'scripts/build-hotfix-0.3.0-12.sh'
      - 'scripts/validate-hotfix-0.3.0-12.sh'
      - '.github/workflows/publish-hotfix-0.3.0-12.yml'

permissions:
  contents: write

concurrency:
  group: mechos-hotfix-12-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout MechOS
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install bundle tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y zstd
      - name: Build and validate Hotfix 12
        run: |
          set -euo pipefail
          bash scripts/build-hotfix-0.3.0-12.sh
          bash scripts/validate-hotfix-0.3.0-12.sh
      - name: Publish Hotfix 12 to Update Center stable channel
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            updates/bundles/MechOS-0.3.0-hotfix.12-update.tar.zst \
            updates/bundles/MechOS-0.3.0-hotfix.12-update.tar.zst.sha256 \
            updates/stable.json
          if git diff --cached --quiet; then
            echo 'Hotfix 12 is already published.'
            exit 0
          fi
          git commit -m 'Publish MechOS v0.3.0 Hotfix 12'
          git reset --hard HEAD
          git clean -fd
          for attempt in 1 2 3; do
            git fetch origin main
            if git rebase origin/main && git push origin HEAD:main; then
              exit 0
            fi
            git rebase --abort >/dev/null 2>&1 || true
            git reset --hard HEAD
            git clean -fd
            sleep $((attempt * 2))
          done
          echo 'Unable to publish Hotfix 12 after three synchronized attempts.' >&2
          exit 1
